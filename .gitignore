# 依赖
node_modules/
target/

# 构建产物
dist/
dist-ssr/
build/
*.local

# Tauri 构建产物
src-tauri/target/
src-tauri/gen/
# 遗留的隔离 cargo target 目录（例如 src-tauri/target-verify）。
# **不应再产生**：app 正在运行导致 exe 被锁时，按 D51 / P4 向客户求助退出实例，
# 而不是改用备用 target 旁路编译。此处忽略只为兜底防误入库，见
# docs/decisions.md D51 与 docs/development-troubleshooting.md。
src-tauri/target-*/

# 编辑器
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# 操作系统
.DS_Store
Thumbs.db

# 日志
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 环境与凭据（安全红线：禁止提交任何真实凭据）
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*
credentials.json

# 本地测试临时文件（含测试私钥，禁止入库）
.tmp-test/
.tmp/

# 数据库
*.sqlite
*.sqlite3
*.db

# 本地专属配置（真实环境测试变量值等），禁止入库
AGENTS.local.md
