# 更新日志

本项目的所有重要变更都会记录在此文件中。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v0.2.0] - 2026-09-24

本轮主题是**提权的身份边界**：允许一个"登录即已是特权用户"的身份免去 `sudo` 这一步。
**本次含数据库结构变更**（首个会触发迁移的版本），覆盖安装后首次打开时自动升级，详见下面的「变更」。

### 新增

- 认证信息可标记「该身份是特权用户」：登录该身份本身就已是特权执行，「提权」这一步因此消失——
  主机不再要求配置提权口令，特权通道也不再经过 `sudo`，直接按该主机的环境加载方式执行。
  该标记由使用者**声明**（不是一个需要填写的 uid），实际 uid 仍由主机侧核实，不符时如实告警；
  它落在认证信息上，所有引用该凭据的主机一起生效，不必逐台修改。
  此路径不受 `Defaults requiretty` 限制（该选项约束的是 `sudo` 的行为）。

### 变更

- **数据库结构变更**：认证信息表新增「是否特权身份」一列（只增列，不重建表），
  打开旧库时自动迁移，已有主机、终端与命令历史原样保留，加密字段不重新加密。
  存量凭据默认按「普通身份」处理。
  退回旧版本也不会读不到数据：多出来的这一列旧版本不认识，会被直接忽略。

### 修复

- 升级被打断后可能起不来：迁移写「结构版本号」与补那一列是两步，若恰好在两步之间被强制结束，
  下次打开会撞上报错、再也进不去。现在补列只看表的实际形状、版本号只往上写，
  版本号与表形状不一致的两种情况都会在下一次打开时自行修好。
- 全局提示互相顶掉：右下角的错误与成功提示原先共用两个单槽，后到的一条会静默顶掉先到的，
  用户漏看提示；且只按固定时长消失，长提示读不完就没了、关闭按钮来不及点。
  现在提示按到达顺序堆叠，鼠标移入即冻结倒计时、移出后只补上暂停时剩下的时长。
- 「发现新版本」的下载入口改为跳转该版本的**发布页**：原先直接给本平台安装包 `.msi` 的直链，
  等于替用户挑了安装包格式（同一发布页下并列 `.msi` 与 `setup.exe`），
  且清单缺少当前平台条目时用户只剩一句提示、没有任何可点入口。
  现在按钮为「前往发布页下载」，选哪个包由用户决定；清单未提供发布页地址时按 tag 推导，
  老清单同样可用。SHA256 一行已明确标注只对应当前平台的安装包。

### 已知边界

- 标记为特权身份后，Agent 的**普通命令**也以该身份执行；主机的「不允许提权」策略
  只关闭 `run_as_root` 这一个入口，命令历史也尚未把这类普通命令标注为特权执行。
- 真实以 root 登录的主机**尚未做端到端实测**（测试环境需要额外授权），
  该路径目前只有进程内用例覆盖。

## [v0.1.2] - 2026-09-21

修复四处界面与状态缺陷。本次不涉及数据库结构变更，覆盖安装即可，已有数据不受影响。

### 修复

- 编辑主机时「环境加载方式」显示为空白：该字段的 JSON 名字由程序内部的变体名推导成了
  `login_then_task`，而下拉框选项与数据库里存的值一律是 `login` / `clean`，回填因此匹配不到选项。
  现在三处共用同一套词表，并加了契约测试守住这条一致性。
- 已经是最新版本却仍提示"发现新版本 <当前版本号>"：更新结果的缓存只按时间（24 小时）判新鲜，
  没有记录这份结论是**为哪个版本**算出来的，于是升级之后仍沿用升级前的结论。
  现在缓存按应用版本作废，版本一变就重新检查。
- 无扩展名的私钥选不中：「导入文件」的选择器按扩展名过滤（pem / key / ppk / txt），
  而 OpenSSH 私钥按惯例没有扩展名（`~/.ssh/id_ed25519`）、公钥是 `.pub`，两者都不在其中，
  于是这些文件在选择器里根本不出现。现在选择器列出全部文件，格式判断交给文件内容——
  读到 PuTTY 的 `.ppk` 仍会明确报错并给出转换出路。
- 错误提示被表单弹窗盖住：提示挂在带 `z-index` 的外壳容器内，那会新建一层层叠上下文，
  容器内的层级数值再大也越不出去。现在提示与弹窗一样挂到页面顶层，始终可见。

## [v0.1.1] - 2026-09-20

修复"无法添加 SSH 主机"。本次不涉及数据库结构变更，覆盖安装即可，已有数据不受影响。

### 修复

- 填过端口就保存不了主机：端口输入框交出的是文本，整数校验把 `22` 这样的合法端口判为非法，
  表单始终提示"端口必须是 1–65535 之间的整数"；现在先把端口归一成数值再校验。
- 提示"已添加 / 已保存"，数据其实没有落库：后端返回失败时信封格式与前端约定不符，
  前端把所有失败都当成成功、错误原因同时被丢弃。**这一条不限于添加主机**——
  此前所有管理操作的失败都会被伪装成成功，界面上既看不到失败也看不到原因；现在如实显示。
  - 随之可见的变化：用主密码保护凭据且尚未解锁时保存主机会明确提示"凭据尚未解锁，
    请先在应用中输入主密码"，解锁后即可正常保存。

## [v0.1.0] - 2026-09-19

首个公开版本。

mf-perch 是一款 Windows 桌面应用，让 AI Agent 通过内嵌的 MCP Server
安全地经 SSH 操作主机，人类保留完整的凭据控制权与命令审计权。

### 新增

- SSH 主机与凭据管理：密码 / 私钥两种认证，凭据字段级加密存储，对 AI Agent 不可见；
- 常驻终端会话：`cd` / `export` 等 shell 状态跨命令保留，支持同步执行与长任务异步查询；
- 内嵌 MCP Server：Bearer Token 鉴权，可接入任意 MCP 客户端；
- sudo 提权策略（`deny` / `ask` / `auto`），提权密码走独立通道，不进入 Agent 可见域；
- 命令历史与终端审计：AI 执行的每条命令可查，人类可删除、AI 仅可归档；
- 应用内版本更新检查（提示 + 跳转下载，不自动安装）；
- 系统托盘、暗色 / 亮色双主题。

[v0.1.2]: https://github.com/mangfu26/mf-perch/releases/tag/v0.1.2
[v0.1.1]: https://github.com/mangfu26/mf-perch/releases/tag/v0.1.1
[v0.1.0]: https://github.com/mangfu26/mf-perch/releases/tag/v0.1.0
